{
	"author": "ITBA",
	"description": "A hand-crafted recursive descent recognizer in TypeScript.",
	"devDependencies": {
		"@types/node": "^26.5.0",
		"husky": "^9.1.7",
		"prettier": "^3.9.6",
		"rimraf": "^6.1.3",
		"tsx": "^4.23.13",
		"typescript": "^7.0.2"
	},
	"engines": {
		"node": ">=26",
		"npm": ">=11"
	},
	"license": "SEE LICENSE IN LICENSE.md",
	"name": "@itba/recursive-descent-recognizer",
	"private": true,
	"repository": {
		"type": "git",
		"url": "git+https://github.com/Alpha-Theta-Gamma-Mu/Recursive-Descent-Recognizer.git"
	},
	"scripts": {
		"build": "rimraf .build .tsbuildinfo && npx tsc",
		"clean": "rimraf .build .format node_modules .tsbuildinfo",
		"coverage": "echo There are no coverage analysis at the moment.",
		"format": "prettier --cache --cache-location .format/prettier --cache-strategy content --list-different --write \"**/*.{json,md,ts,yaml}\"",
		"lint": "npm pkg fix && npm audit --audit-level=low",
		"prepare": "husky",
		"start": "tsx src/main/typescript/EntryPoint.ts",
		"test": "echo There are no tests at the moment.",
		"verify": "npm pkg fix && npm audit --audit-level=low && prettier --cache --cache-location .format/prettier --cache-strategy content --check \"**/*.{json,md,ts,yaml}\""
	},
	"type": "module",
	"version": "0.1.0"
}
